// index.js

// circle.js dosyasından fonksiyonları al
const { circleArea, circleCircumference } = require('./circle');

// Yarıçap
const r = 5;

// Fonksiyonları kullan
circleArea(r);
circleCircumference(r);

